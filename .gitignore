# Dependencies
node_modules/
mobile/node_modules/

# Build output
dist/
mobile/dist/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging

# Expo / React Native
mobile/.expo/
mobile/android/
mobile/ios/
mobile/.env
mobile/.env.local

# Test output
coverage/
test-results/
playwright-report/
blob-report/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Claude / Agents (local tooling — not project source)
.claude/
.agents/
skills-lock.json

# Mobile signing artifacts (managed by EAS cloud — never commit local copies)
*.jks
*.keystore
*.p12
*.cer
*.mobileprovision
google-services.json
GoogleService-Info.plist

# Mobile build artifacts — APK/IPA/AAB produced by local builds; EAS builds are cloud-only
*.apk
*.ipa
*.aab
